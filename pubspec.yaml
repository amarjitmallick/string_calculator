name: string_calculator
description: An implementation of the classic String Calculator Kata in Dart, using Test Driven Development (TDD).
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
